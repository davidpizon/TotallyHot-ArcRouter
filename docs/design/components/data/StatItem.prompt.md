One metric in a stat strip — the atomic unit repeated across turn cards and the pinned conversation summary (ROI, Cost, Tok P/C, Steps, Cache, TTFT, Ctx, Model).

```jsx
<StatItem label="ROI" value="85.0%" tooltip="Return on investment vs. worst-case model" valueColor="var(--status-ok-text)" />
```

Always monospace for the value — this product puts every number in JetBrains Mono, never Inter.
