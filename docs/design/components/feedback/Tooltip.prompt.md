A hover tooltip for defining dense metric labels (ROI, TTFT, cache hit rate) inline without cluttering the layout.

```jsx
<Tooltip label="Return on investment vs. the worst-case model">
  <span className="ls-stat-label">ROI</span>
</Tooltip>
```

Used throughout the Live Stream tab's stat strips — every abbreviation gets one.
