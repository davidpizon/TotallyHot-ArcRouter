# Historical React design kit

> **Not runtime.** Production UI is Blazor in `TotallyHotArcRouter.Gui.Components` (Razor library)
> and `TotallyHotArcRouter.Gui.Web` (WASM host served by the router). The Windows companion is
> `TotallyHotArcRouter.Tray` (WinForms), not a MAUI tray dashboard.
>
> **Live visual contract:** [`../gui/DESIGN.md`](../gui/DESIGN.md) and [`../gui/MOTION.md`](../gui/MOTION.md).
>
> The React reconstruction that used to live in this folder (tokens, `components/*.jsx`,
> `ui_kits/dashboard/`) is archived at [`../archive/design/`](../archive/design/). Do not copy it
> into production, and do not treat it as the dashboard implementation.
