A dark-filled text/number input with a sky-blue focus ring — used for Governance's editable budget cap and the Settings confirmation-word fields.

```jsx
<Input type="number" placeholder="500" />
```

Focus swaps the border from slate-700 to sky-400; no box-shadow glow.
